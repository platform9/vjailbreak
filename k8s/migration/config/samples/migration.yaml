apiVersion: vjailbreak.k8s.pf9.io/v1alpha1
kind: Migration
metadata:
  labels:
    app.kubernetes.io/name: migration
    app.kubernetes.io/managed-by: kustomize
  name: vm-migration-sample
spec:
  source:
    vmwareref: pnapbmc1 #(ip, username, password)
    datacenter: PNAP BMC
    virtualmachines:
    - dev test1
    - dev test2
  destination:
    openstackref: pnappmo1 #(admin.rc vars)